import React from 'react';

import CardsPage from '../../scenes/CardsPage';

const MainContent = (props) => {
    return (
        <div className='main-content'>
            <CardsPage />
        </div>
    );
};

export default MainContent;
